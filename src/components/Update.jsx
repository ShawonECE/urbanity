import { Helmet } from "react-helmet-async";

const Update = () => {
    return (
        <div>
            <Helmet>
                <title>Update Profile</title>
            </Helmet>
            Update your profile
        </div>
    );
};

export default Update;